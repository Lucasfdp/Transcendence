# Portainer tools directory

# ─────────────────────────

# Portainer uses its own built-in entrypoint and requires no custom scripts.

# This directory is a placeholder to maintain the project structure.

#

# If you need to pre-configure Portainer (e.g. create an admin user via API),

# place a setup script here and call it from a one-shot init container.
